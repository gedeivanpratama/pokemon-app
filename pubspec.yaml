name: flutter_technical_test
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.1.2
  flutter_riverpod: ^2.3.6
  equatable: ^2.0.5
  dartz: ^0.10.1
  connectivity_plus: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  custom_lint: ^0.4.0
  riverpod_lint: ^1.3.2
  mockito: ^5.4.0
  build_runner: ^2.4.4
  http_mock_adapter: ^0.4.4

flutter:
  uses-material-design: true

